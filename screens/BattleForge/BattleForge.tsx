import React,{ Text, View } from 'react-native';
import styles from '../../src/style/battleForgeStyle';

const BattleForge = () => { 
  
  
      return (
          <View style={styles.scrollContainer}>
              <Text>Ici sera la page de Création d'armée</Text>
          </View>
      );
  };

  export default BattleForge;